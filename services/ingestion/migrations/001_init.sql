-- 001_init.sql
-- Sprint 1: trivial schema-creation statement only. Real tables (enums,
-- locations, fee_categories, raw_transactions, dashboard_transactions,
-- daily_revenue, etc.) are Sprint 2 scope -- see
-- .claude/sprints/02_SCHEMA_MIGRATIONS.md. Do not add tables here.

CREATE SCHEMA IF NOT EXISTS eparking;
